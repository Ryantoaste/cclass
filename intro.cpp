#include <iostream>
#include <cstdlib>
#include <ctime>

using namespace std;
void other() {
    cout << "it works";
}
int main(){
   /*int input = 0;
   char plus;
   int secint = 0;
   cin >> input >> plus >> secint;
   cout << input << endl;
   cout << plus << endl;
   cout << secint << endl;
   cout << "=" << endl << "21";
   cout.setf(ios::showpoint);
   cout.setf(ios::fixed, ios::floatfield);
   cout.precision(2);
   float longput = 32423424.000;
   cout << longput << endl;
   cout.unsetf(ios::showpoint);
   srand((unsigned)time(0));
   int i;
   i = (rand()%6)+1;
   cout << i << endl;*/
   int may = 0;
   cin >> may;
   if (may == 3)
   {
    cout << "no" << endl;
   }
   other();
   return 0;
}

